import { Color } from 'three';

import theme from '~/theme';

const orbitColors = {
  main: new Color(theme.colors.main),
  error: new Color(theme.colors.error),
  success: new Color(theme.colors.success),
  white: new Color('#ffffff'),
  gray: new Color('#cccccc'),
};

export default orbitColors;