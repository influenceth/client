import styled from 'styled-components';

const StyledDetails = styled.div`
  bottom: 0;
  left: 0;
  position: absolute;
  right: 0;
  top: 0;
`;

const Details = () => {
  return (
    <StyledDetails />
  );
};

export default Details;
